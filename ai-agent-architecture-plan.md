# 环境部署 AI Agent 架构规划

## 1. 目标定义

这个项目的核心目标不是做一个“会聊天的运维助手”，而是做一个“能把部署需求转成受控执行任务”的服务器 Agent。

进一步对齐后的定位是：它首先是一个轻量级 Agent 框架，其次才是一个具体的部署产品。也就是说，重点不是先做大而全的平台，而是先做一个可以长期驻留在服务器上的“环境部署管家”。

它需要满足四个约束：

1. 运行在服务器上，可部署在本地机房或云服务器。
2. 服务器联网，可调用第三方 LLM API。
3. 接收自然语言部署需求，并转成标准化部署计划。
4. 所有真正落到服务器上的动作都必须经过受控执行层，而不是让 LLM 直接拼接任意命令执行。

### 1.1 当前需求对齐

按你现在的定义，这个项目更接近下面这个形态：

1. 它是一个常驻在服务器上的本地 Agent。
2. 它服务的是“服务器环境部署和环境维护”这个单一领域。
3. 它应该尽量轻，不依赖复杂的分布式基础设施。
4. 它应该优先支持单机自治，而不是先做多租户平台。
5. 它应该像服务器上的“大管家”，能理解需求、给出计划、执行受控动作并回报结果。

因此，第一版不应该默认要求：

1. 完整的前后端平台。
2. 微服务拆分。
3. 多节点调度中心。
4. 重型工作流引擎。

第一版更合理的目标是：单服务进程加本地执行器，加少量可插拔模块。

## 2. 总体原则

建议采用“LLM 负责理解和规划，执行器负责落地和校验”的双层架构。

原则如下：

1. LLM 不直接拥有系统权限。
2. 所有部署动作必须映射为白名单任务或受控脚本。
3. 所有执行步骤必须可审计、可回放、可中断、可重试。
4. 高风险动作必须需要人工确认。
5. Agent 必须维护环境状态，而不是每次都从零推理。

## 3. 推荐系统分层

这里的“分层”更适合理解为逻辑模块，而不一定要拆成独立服务。对于轻量级框架，第一版完全可以是单进程应用，内部按模块分层。

### 3.1 接入层

负责接收外部需求。

输入来源建议先保留最少集合：

1. 本地 HTTP API
2. 命令行客户端

后续再扩展：

1. Web 控制台
2. 企业微信或飞书机器人

这一层只做三件事：

1. 接收请求
2. 做身份认证
3. 把请求转成统一任务格式

### 3.2 Agent 编排层

这是整个系统的大脑，但不是直接执行器。

它负责：

1. 理解用户意图
2. 判断目标环境类型
3. 选择部署模板
4. 生成执行计划
5. 拆解成可执行步骤
6. 汇总结果并反馈

在轻量版本里，不建议把这些角色做成多个 Agent 进程，先作为同一进程内的几个模块：

1. Requirement Parser：把自然语言需求转成结构化参数。
2. Planner：生成部署步骤草案。
3. Policy Checker：校验是否违反安全策略。
4. Executor Dispatcher：把步骤分发给执行器。
5. Report Generator：输出部署报告。

### 3.3 策略与安全层

这是最关键的一层，用来约束 LLM 输出。

至少要包含：

1. 命令白名单
2. 文件路径白名单
3. 端口开放策略
4. 服务注册策略
5. 高风险操作审批规则
6. Secret 使用规则

例如：

1. 允许创建目录、写配置、安装特定包、注册服务。
2. 默认禁止删除系统目录、清空磁盘、修改管理员密码、关闭防火墙全部规则。
3. 开放 80、443 可以自动执行，但开放 3389、22 需要审批。

### 3.4 执行层

执行层不关心自然语言，它只接收结构化步骤。

建议执行层采用“任务插件 + 系统适配器”的方式：

1. Windows Adapter：执行 PowerShell、服务注册、Nginx 配置、Windows 防火墙规则。
2. Linux Adapter：执行 Shell、systemd、iptables 或 ufw、Nginx 配置。
3. Cloud Adapter：调用云厂商 API，比如安全组、弹性 IP、对象存储、负载均衡。

执行层中的每一步应该具有固定结构：

1. step_id
2. action_type
3. target
4. arguments
5. precheck
6. execute
7. verify
8. rollback

这样可以避免“直接生成一大段命令然后盲跑”。

### 3.5 状态与资产层

这层负责让 Agent 记住当前环境到底是什么状态。

建议存储以下信息：

1. 服务器基础信息：OS、CPU、内存、磁盘、IP。
2. 已安装软件：Node、Nginx、Docker、Python、数据库版本。
3. 已开放端口。
4. 已注册服务。
5. 已部署应用版本。
6. 配置文件版本。
7. 最近一次部署结果。

轻量版不需要一开始就上复杂存储，可以按阶段演进：

1. 第一版：SQLite 或本地 JSON 文件，保存任务、状态、审计记录。
2. 第二版：PostgreSQL，适合并发更高或远程集中管理。
3. 文件存储：直接保存日志、生成配置、回滚材料。

### 3.6 观测与审计层

这是后期可运维的基础。

至少记录：

1. 谁发起了部署
2. LLM 生成了什么计划
3. 策略层拦截了哪些动作
4. 实际执行了哪些命令或脚本
5. 每一步的输出、错误、耗时
6. 最终验证是否通过

建议输出两类日志：

1. 面向机器的结构化日志
2. 面向人的部署报告

## 4. 核心执行链路

建议把一次部署任务定义为以下流程：

1. 用户提交需求
2. Agent 解析需求，提取目标系统、运行环境、暴露端口、依赖组件、部署方式
3. Planner 生成结构化计划
4. Policy Checker 做安全校验
5. 如果存在高风险步骤，进入人工审批
6. 执行层按步骤执行 precheck
7. precheck 通过后执行真正动作
8. 每一步执行后做 verify
9. 任一步失败则触发回滚或中止策略
10. 生成完整报告

对于你现在强调的“轻量级服务器管家”形态，这条链路在运行时里应该体现为一个受控 Agent Loop，而不是一次性把全部命令吐出来再执行。

建议采用下面这个最小闭环：

1. Observe：读取用户需求、本机状态、任务上下文。
2. Think：由 LLM 生成当前最小下一步，而不是完整无限长计划。
3. Call Tool：调用一个受控工具。
4. Verify：检查工具结果是否符合预期。
5. Record：把结果写入状态存储。
6. Continue or Stop：决定继续下一步、请求确认、还是结束任务。

这就是第一版最重要的 Agent Loop。

用一个简化的任务结构表示，大致如下：

```json
{
  "task_type": "deploy_node_service",
  "environment": {
    "os": "windows-server-2022",
    "provider": "tencent-cloud"
  },
  "requirements": {
    "runtime": "nodejs-lts",
    "reverse_proxy": "nginx",
    "service_wrapper": "nssm",
    "ports": [80, 443],
    "app_dir": "D:\\ar-magnet\\app",
    "asset_dir": "D:\\ar-assets\\magnets"
  },
  "steps": [
    {
      "action_type": "ensure_directory",
      "target": "D:\\ar-magnet\\app"
    },
    {
      "action_type": "install_runtime",
      "target": "nodejs-lts"
    },
    {
      "action_type": "configure_nginx",
      "target": "D:\\nginx\\conf\\nginx.conf"
    }
  ]
}
```

## 5. LLM 在系统中的职责边界

推荐把 LLM 的职责限制在以下范围：

1. 理解自然语言部署需求
2. 提取缺失参数并追问
3. 选择合适的部署模板
4. 生成结构化计划
5. 解释失败原因
6. 输出面向人的总结报告

不建议让 LLM 直接负责：

1. 直接产生命令并立刻执行
2. 自由修改任意配置文件
3. 直接读取所有 Secret 原文
4. 绕过审批流程执行高风险操作

更准确地说，LLM 应该像“计划器”，不是“root shell”。

## 5.1 Agent Loop 设计原则

如果项目要具备 Agent Loop 和 Tool Calling，那么建议明确采用“小步执行”的策略，而不是一次规划几十步。

原因很直接：

1. 环境部署是强状态依赖任务。
2. 前一步是否成功，会直接改变后一步是否成立。
3. 安装类任务很容易受到网络、权限、版本源、磁盘空间影响。
4. 小步执行更容易做安全控制和错误恢复。

因此运行时建议遵循下面三个规则：

1. 一次只让模型决定当前一个动作或一小组同类动作。
2. 每次工具调用后都必须读取真实结果，再决定下一步。
3. 如果工具结果和预期不一致，优先走诊断分支，而不是盲目重试。

## 5.2 Tool Calling 设计原则

这个项目需要 Tool Calling，但不应该做成无限制通用工具市场。更适合做成“部署领域专用工具箱”。

推荐的工具分层如下：

1. Inspect 类工具：采集环境状态。
2. Plan 类工具：把模板和参数拼成步骤草案。
3. Execute 类工具：执行受控动作。
4. Verify 类工具：验证结果。
5. Report 类工具：生成部署结论。

一个关键原则是：

LLM 只能选择工具和填写参数，不能直接注入任意原始命令。

例如推荐这样：

1. install_python(version="3.11.9")
2. ensure_directory(path="D:\\runtime\\python")
3. verify_python_version(expected="3.11.9")

而不是这样：

1. run_command(command="curl ... ; msiexec ... ; setx ...")

如果必须存在通用命令工具，也应该：

1. 默认关闭。
2. 仅管理员可用。
3. 受命令白名单和参数校验限制。
4. 全量审计。

## 6. 与参考文档的映射关系

你当前工作区里的部署文档已经很适合沉淀成第一版任务模板，尤其适合作为 Windows 场景的 MVP。

以其中的 Windows Server 2022 部署步骤为例，可以直接映射成标准动作：

1. 创建目录，对应 ensure_directory。
2. 安装 Node.js，对应 install_runtime。
3. 安装 Nginx，对应 install_package 或 unpack_binary。
4. 配置防火墙，对应 open_firewall_port。
5. 上传后端代码，对应 sync_artifact。
6. 执行 npm install，对应 run_app_dependency_install。
7. 生成 .env，对应 write_config_file。
8. 启动 Node 服务，对应 run_process_for_validation。
9. 写 Nginx 配置，对应 configure_reverse_proxy。
10. 用 NSSM 注册服务，对应 register_windows_service。
11. 验证 /api 和 /static，对应 verify_http_endpoint。

这意味着你的第一版不需要做“通用全能 Agent”，而是先做“模板驱动 + LLM 补参数”的部署 Agent。

这条路线更稳。

## 7. 推荐的 MVP 版本边界

第一版建议只支持少量高价值场景，不要一开始追求全自动全平台。

推荐 MVP 范围：

1. 仅支持单机部署，不做多机编排。
2. 仅支持 Windows Server 2022 和 Ubuntu 22.04。
3. 仅支持三类应用：Node API、静态资源站点、Nginx 反向代理。
4. 仅支持白名单模板，不支持任意自定义 shell。
5. 高风险操作必须人工确认。
6. 先支持同步部署，不急着做复杂的异步工作流。

这样更容易把系统做稳定。

## 8. 推荐模块设计

如果你要真正开始实现，建议按“轻量框架”的方式拆成下面这些模块。第一版不需要把它们做成多个独立服务，放在一个代码仓库甚至一个进程里都可以。

### 8.1 agent-runtime

负责 Agent 进程生命周期管理。

包括：

1. 启动配置加载
2. Provider 初始化
3. 执行器初始化
4. 本地状态恢复
5. 调度循环

### 8.2 request-gateway

负责接入请求、鉴权、任务管理、状态查询。

### 8.3 agent-core

负责需求解析、计划生成、策略校验、任务编排。

其中最重要的内部职责应该包括：

1. loop-controller：驱动 Observe -> Think -> Act -> Verify 闭环。
2. tool-router：根据模型输出分发到正确工具。
3. context-builder：拼装用户需求、本机状态、最近工具结果。
4. failure-handler：遇到失败时决定重试、诊断、回滚或人工确认。

### 8.4 execution-engine

负责执行结构化任务步骤。

### 8.5 adapters

按平台拆分：

1. adapters/windows
2. adapters/linux
3. adapters/cloud

### 8.6 template-library

保存部署模板，例如：

1. deploy-node-api
2. deploy-static-site
3. setup-nginx-reverse-proxy
4. register-windows-service

### 8.7 policy-engine

集中处理命令白名单、审批规则、路径限制、端口限制。

### 8.8 state-store

保存环境状态、任务状态、步骤执行记录。

### 8.9 audit-reporter

生成人类可读的部署结果和失败分析。

### 8.10 local-knowledge

保存本机环境快照和可复用经验，例如：

1. 已探测到的软件版本
2. 上一次成功部署参数
3. 常用目录约定
4. 本机允许的动作清单

这部分很像“服务器记忆”，能让 Agent 更像本地大管家，而不是每次都重新问一遍环境信息。

## 9. 第三方 LLM 接入建议

因为服务器联网，完全可以直接用第三方 API Key 模式接入模型。

建议把模型抽象成独立 Provider 层，至少支持：

1. OpenAI 兼容接口
2. Anthropic 接口
3. Azure OpenAI
4. 国内兼容 OpenAI 协议的模型服务

Provider 层需要统一：

1. chat 接口
2. function calling 或 tool calling
3. 超时控制
4. 重试机制
5. 成本统计
6. fallback 模型切换

建议策略：

1. 规划阶段用更强模型。
2. 总结、报告、参数补全可用便宜模型。
3. 高风险任务时强制输出结构化 JSON，而不是自由文本。

对于 Tool Calling，推荐统一成结构化响应，例如：

```json
{
  "thought": "需要先确认当前主机是否已安装目标版本 Python。",
  "tool_name": "inspect_python",
  "arguments": {
    "expected_version": "3.11.9"
  },
  "stop": false
}
```

这样更容易做运行时校验和追踪。

轻量版还有一个关键点：LLM Provider 必须可以热切换。也就是说，框架不应绑定某一家模型服务，而应该允许通过配置切换不同 API Key、Base URL 和模型名。

## 10. 安全设计建议

这个项目真正的难点不是“能不能执行命令”，而是“怎么避免执行出事”。

建议最少做以下安全控制：

1. 执行账户最小权限化。
2. Agent 主进程与执行器分权。
3. Secret 存储与日志脱敏。
4. 命令模板参数化，禁止字符串拼接执行。
5. 风险分级：低风险自动执行，中风险需确认，高风险禁止。
6. 配置文件写入前做 schema 校验。
7. 外部下载做来源白名单和校验和校验。
8. 对所有破坏性操作要求二次确认。

## 11. 推荐技术路线

如果你的目标是尽快做出第一版，推荐两种路线。

### 方案 A：Node.js 为主

适合你参考文档里的 Node/Nginx 场景。

推荐组合：

1. API 层：NestJS 或 Fastify
2. Agent 编排：TypeScript
3. 执行器：PowerShell + Bash 适配器
4. 数据库：PostgreSQL
5. 队列：BullMQ 或基于数据库的简化任务队列
6. 日志：Pino

优点是和部署目标技术栈接近。

### 方案 B：Python 为主

适合后续做更多 Agent 编排、工具调用和工作流管理。

推荐组合：

1. API 层：FastAPI
2. Agent 编排：Python
3. 执行器：PowerShell Remoting、SSH、Shell
4. 数据库：PostgreSQL
5. 队列：Celery 或 Arq
6. 日志：structlog

优点是生态里更容易接 Agent 框架和自动化能力。

如果你当前团队更偏 Node，我建议先走方案 A，但实现形态要尽量克制：一个主进程，内部模块化，外加少量系统脚本。

## 11.1 更贴近当前需求的实现形态

如果用一句话概括，第一版最合适的实现形态是：

一个常驻服务进程，一个本地任务队列，一个模板库，一个受控执行器，加一个可替换的 LLM Provider。

这比“平台化架构”更符合轻量级 Agent 框架的定位。

## 12. 建议的任务状态机

部署任务建议使用明确状态机，而不是只存一个 success 或 failed。

例如：

1. created
2. parsing
3. planning
4. waiting_approval
5. queued
6. running
7. verifying
8. succeeded
9. failed
10. rolled_back
11. cancelled

这样后续做 UI、审计、重试都更稳。

## 13. 后续演进路线

建议分 4 个阶段推进。

### 阶段 1：模板化 MVP

1. 支持固定模板。
2. 支持第三方 LLM 补参数。
3. 支持 Windows 单机部署。
4. 支持部署报告。

### 阶段 2：多平台执行器

1. 增加 Linux Adapter。
2. 支持 systemd、Nginx、Docker。
3. 支持远程主机执行。

### 阶段 3：云资源联动

1. 接入云 API。
2. 自动配置安全组、域名、证书、对象存储。
3. 支持部署前资源检查。

### 阶段 4：闭环运维

1. 支持增量发布。
2. 支持回滚包。
3. 支持异常自愈。
4. 支持巡检任务。

## 14. 最终建议

这个项目最适合的起点不是“做一个通用超级 Agent”，而是：

1. 先把参考文档里的 Windows Server 部署流程产品化。
2. 把每个步骤沉淀成结构化动作模板。
3. 让 LLM 只负责理解需求、补参数、选模板、生成报告。
4. 把真正危险的系统操作关进策略层和执行层。

如果进一步按你现在的表述收口，我建议把项目定义成：

一个可插拔、可本地运行、可调用第三方 LLM 的轻量级环境部署 Agent 框架。

它的首要能力不是“无所不能”，而是：

1. 像服务器管家一样理解部署需求。
2. 知道本机当前状态。
3. 只执行被允许的动作。
4. 能把部署过程和结果讲清楚。

如果再进一步压缩成实现口径，第一版最值得优先做的是：

1. 一个稳定的 Agent Loop。
2. 一组小而硬的部署工具。
3. 一个模板优先的规划器。
4. 一个可审计的执行记录系统。

如果按这个方式推进，你会得到一个真正能上线的部署 Agent，而不是一个只能演示的命令生成器。